---
title: الارتباط التلقائي لـ STR (داخلي)
description: كيف يكتشف محرك ارتباط STR التلقائي أنماط تزامن علامات التهيكل وخرق السرعة، ويصيغ تقارير SAR، ويُرسل إشعارات الشركاء.
navigation:
  title: الارتباط التلقائي لـ STR
---

::callout{type="warning"}
**داخلي — مقيد**. للموظفين في الامتثال والهندسة فقط.
::

# الارتباط التلقائي لـ STR

## نظرة عامة

يكتشف `AutoStrCorrelationService` حين تُطلَق علامتا AML `structuring` و`velocity_breach` على نفس الكيان خلال نافذة متداولة مدتها 72 ساعة. عند اكتشاف هذا النمط، تُنشأ مسودة تقرير نشاط مشبوه (SAR) تلقائيًا ويُبلَّغ الشريك.

الـ Auto-STR أداة امتثال مساعدة — تُقلل من فرصة إغفال نمط تزامن العلامات أثناء المراجعة اليدوية. يبقى SAR بحالة `draft` ولا يُقدَّم تلقائيًا قط.

---

## شروط الإطلاق

يُنشأ SAR تلقائيًا حين تتحقق **جميع** الشروط التالية:

1. علامة `structuring` موجودة لـ `entity_uuid` خلال آخر `WINDOW_HOURS` (الافتراضي: 72 ساعة)
2. علامة `velocity_breach` موجودة لنفس `entity_uuid` خلال نفس النافذة
3. لا يوجد SAR قائم بحالة `open` أو `draft` أو `under_review` لهذا الكيان خلال نافذة إزالة التكرار (`DEDUP_HOURS`، الافتراضي: 168 ساعة / 7 أيام)

تمنع نافذة إزالة التكرار إنشاء SARs مكررة للأنماط المستمرة متعددة الأيام.

---

## تكوين الخدمة

```php
// apps/core/config/aml.php
'auto_str' => [
    'window_hours' => env('AUTO_STR_WINDOW_HOURS', 72),
    'dedup_hours'  => env('AUTO_STR_DEDUP_HOURS', 168),
    'enabled'      => env('AUTO_STR_ENABLED', true),
],
```

سيُهاجَر كلا الثابتين إلى `configurable_rules` المدعومة من قاعدة البيانات في المرحلة R.

---

## إنشاء مسودة SAR

عند الإطلاق، يستدعي `AutoStrCorrelationService::checkAndDraft()` دالة `SARService::draft()` مع:

```php
$sar = $this->sarService->draft($partnerUuid, $entityUuid, [
    'trigger'          => 'auto_str_correlation',
    'structuring_flag' => $structuringFlag->uuid,
    'velocity_flag'    => $velocityFlag->uuid,
    'window_hours'     => $this->windowHours,
]);
```

تحتسب `SARService::draft()` مسبقًا:
- إجمالي عدد المعاملات والمبلغ الإجمالي خلال النافذة
- قائمة معرفات UUID للإشارات المساهمة
- جزء سرد أولي (يُملأ بواسطة `generateNarrative()`)

---

## الإشعار

بعد إنشاء المسودة، يُرسَل حدث webhook بـ `compliance.sar.auto_drafted` إلى جميع نقاط نهاية webhook النشطة للشريك:

```json
{
  "event": "compliance.sar.auto_drafted",
  "data": {
    "sar_uuid": "sar_01HZ...",
    "entity_uuid": "ent_01HZ...",
    "trigger": "auto_str_correlation",
    "transaction_count": 6,
    "aggregate_amount": 47200.00,
    "currency": "USD",
    "review_url": "https://app.protegey.com/compliance/sars/sar_01HZ..."
  }
}
```

---

## تفاصيل منطق التزامن

تستعلم الخدمة علامات AML باستخدام JOIN واحد لضمان وقوع كلا نوعَي العلامات خلال النافذة لنفس `entity_uuid` (لا لمجرد نفس الشريك):

```php
$structuringFlag = AmlFlag::where('partner_uuid', $partnerUuid)
    ->where('entity_uuid', $entityUuid)
    ->where('flag_type', 'structuring')
    ->where('created_at', '>=', now()->subHours($this->windowHours))
    ->latest()
    ->first();

$velocityFlag = AmlFlag::where('partner_uuid', $partnerUuid)
    ->where('entity_uuid', $entityUuid)
    ->where('flag_type', 'velocity_breach')
    ->where('created_at', '>=', now()->subHours($this->windowHours))
    ->latest()
    ->first();

if ($structuringFlag && $velocityFlag && !$this->hasDuplicateSar($partnerUuid, $entityUuid)) {
    return $this->sarService->draft(...);
}
```

---

## دورة حياة SAR بعد الإنشاء التلقائي

```
auto_drafted → draft (الشريك يعدّل السرد) → under_review → filed
                                                          ↘ closed (قرار الشريك)
```

يجب على الشركاء:
1. مراجعة SAR المُنشأ تلقائيًا في لوحة الامتثال
2. تعديل السرد إذا لزم (إضافة سياق، تصحيح المبالغ المحتسبة مسبقًا)
3. الانتقال إلى `under_review` عبر `POST /compliance/sars/{id}/review`
4. التقديم إلى الجهة التنظيمية عبر `POST /compliance/sars/{id}/file`

تستخدم حزمة الأدلة المُصدَّرة عند التقديم معرفات الكيانات المجزَّأة بـ HMAC. يجب على الشركاء استبدال التجزئات بالهويات KYC الحقيقية قبل التقديم إلى وحدة الاستخبارات المالية (FIU).

---

## مسار التدقيق

كل إطلاق Auto-STR وكل انتقال لحالة SAR مُسجَّل في `audit_logs` مع:
- `action`: `sar.auto_drafted`، `sar.reviewed`، `sar.filed`
- `actor_type`: `system` (الإنشاء التلقائي)، `user` (الانتقالات اليدوية)
- `metadata`: علامات الإطلاق، معاملات النافذة، UUID مستخدم الممثل

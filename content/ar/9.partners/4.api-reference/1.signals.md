---
title: واجهة برمجة تطبيقات الإشارات (Signals API)
description: إرسال إشارات الاحتيال إلى شبكة Protegey عبر UFES v2
navigation:
  title: واجهة برمجة تطبيقات الإشارات
---

::callout{icon="i-lucide-arrow-right" color="primary"}
**الإشارات = استيعاب.** تضيف هذه الواجهة الأحداث إلى رسم بياني الكيانات وتُعيد درجة مخاطر أولية. للتقييم الفوري للبيانات الخام دون إنشاء كيانات، استخدم [واجهة Evaluate](/partners/api-reference/evaluate) بدلاً من ذلك.
::

تسمح واجهة برمجة تطبيقات الإشارات للشركاء بإرسال مؤشرات الاحتيال إلى شبكة Protegey. يتم **التحقق من صحة كل إشارة وتقييمها بشكل متزامن** — يُعاد حكم المخاطر الأولي في نفس استجابة HTTP. تستمر المعالجة غير المتزامنة (ربط الرسم البياني، وإثراء الكيانات، وتوليد التنبيهات) في الخلفية.

> **هل تبحث عن شيء آخر؟**
> - تقييم فوري دون استمرارية → [واجهة Evaluate](/partners/api-reference/evaluate)
> - استخبارات متعمقة على الكيانات → [واجهة Assess](/partners/api-reference/entities#assess)
> - استيعاب الإشارات في الرسم البياني → [واجهة الإشارات](/partners/api-reference/signals)

::callout{icon="i-lucide-shield-alert" color="amber"}
**الوصول إلى بيئة الإنتاج مطلوب**: يجب أن يكون لديك مفاتيح إنتاج نشطة لنشر الإشارات الحية. إشارات بيئة الاختبار (Sandbox) لا تؤثر على نتائج الشبكة.
::

## نقطة النهاية (Endpoint)

```http
POST /api/v1/signals
```

### الرؤوس (Headers)

| الرأس             | القيمة                        | مطلوب   |
| ----------------- | ----------------------------- | ------- |
| `Content-Type`    | `application/json`            | نعم     |
| `X-Centry-Key`    | `cen_live_sk_...`             | نعم     |
| `Idempotency-Key` | مفتاح فريد (حد أقصى 64 حرفًا) | موصى به |

---

## UFES v2 — الصيغة الوحيدة المدعومة

يجب أن تستخدم جميع عمليات التكامل الجديدة **UFES v2** (`schema_version: "2.0"`). تم حذف الصيغ القديمة وv1 من مسار الاستيعاب.

### الحقول المطلوبة

| الحقل                     | النوع   | الوصف                                                                    |
| ------------------------- | ------- | ------------------------------------------------------------------------ |
| `schema_version`          | `"2.0"` | يجب أن يكون `"2.0"` بالضبط                                               |
| `event_type`              | string  | نوع الحدث الحر (مثلاً: `MOBILE_MONEY_SEND`، `LOGIN_FAILURE`، `SIM_SWAP`) |
| `timestamp`               | string  | تاريخ ووقت بتنسيق ISO 8601                                               |
| `source`                  | object  | بيانات تعريفية لمصدر الإرسال                                             |
| `source.confidence_score` | number  | 0.0–1.0 — مستوى ثقتك في أن هذا الحدث ذو صلة بالاحتيال                    |
| `primary_actor`           | object  | الكيان المنفِّذ للإجراء                                                  |
| `primary_actor.entity_id` | string  | معرفك الداخلي الثابت لهذا الكيان                                         |

::callout{icon="i-lucide-info" color="blue"}
`event_category` **اختياري** — يُستنتج تلقائيًا من `event_type` عند حذفه (مثلاً: `TRANSFER` ← `transaction`، و`LOGIN` ← `auth`، و`SIM_SWAP` ← `auth`). أدرجه بشكل صريح فقط عندما يكون الاستنتاج التلقائي غير ملائم لحالة استخدامك.
::

### المخطط الكامل

```typescript
interface UFESv2Signal {
  // ── Envelope (required) ──────────────────────────────────────────
  schema_version: "2.0";
  event_type: string; // e.g. "MOBILE_MONEY_SEND", "SIM_SWAP", "LOGIN_FAILURE"
  timestamp: string; // ISO 8601

  // ── Envelope (optional) ──────────────────────────────────────────
  event_category?: "transaction" | "auth" | "kyc" | "fraud_signal" | "system";
  flow_type?: "payment" | "auth" | "kyc"; // auto-inferred from event_category if omitted
  trace_id?: string; // your cross-system trace ID

  // ── Source ───────────────────────────────────────────────────────
  source: {
    confidence_score: number; // 0.0–1.0, required
    integration_channel?: "api" | "batch" | "stream";
    partner_user_id?: string; // your internal user reference
    ip_address?: string; // submitting user's IP — feeds IMPOSSIBLE_TRAVEL detector
  };

  // ── Primary actor (required — one plaintext identifier required) ─
  primary_actor: {
    entity_id?: string; // your stable internal ID for this actor
    entity_type?: "individual" | "business" | "account" | "device";
    display_name?: string;

    // ── Plaintext identifiers (submit as-is; Protegey normalizes + hashes) ──
    // At least one of the following is required.
    phone?: string; // E.164 recommended (e.g. "+233241234567"); spaces stripped
    email?: string; // any casing; trimmed + lowercased server-side
    account?: string; // any account reference; trimmed + lowercased server-side
    national_id?: string; // any casing; uppercased + spaces stripped server-side

    // ── Secondary identifiers (all plaintext → all hashed server-side) ────
    identifiers?: {
      msisdn?: string; // alias for phone
      bvn?: string; // 11-digit Bank Verification Number (Nigeria)
      tin?: string; // Tax Identification Number
      passport_number?: string;
      driver_license?: string;
      voter_id?: string;
      pan?: string; // Payment card number (13–19 digits)
      iban?: string;
      wallet_address?: string;
      name?: string; // display name — lowercased + collapsed spaces
      region?: string; // ISO 3166-1 alpha-2 country code (e.g. "GH")
      sim_changed_at?: string; // ISO 8601 — feeds SIM_SWAP_PROXIMITY detector
    };

    // ── Device ────────────────────────────────────────────────────────────
    device?: {
      fingerprint?: string; // opaque fingerprint (max 255 chars) — feeds NEW_DEVICE_ACCESS
      ip_address?: string; // device IP
      user_agent?: string; // raw UA string (max 512 chars)
      imei?: string; // 15 digits — hashed server-side
      sim_iccid?: string; // 18–20 digits — hashed server-side
    };
    risk_flags?: string[]; // e.g. ["high_velocity", "new_device"]
  };

  // ── Related entities (optional) ──────────────────────────────────
  related_entities?: Array<{
    entity_id: string;
    entity_type?: string;
    relationship_type?:
      | "sender"
      | "receiver"
      | "beneficiary"
      | "shared_device"
      | "linked_account"
      | "mule"
      | "director"
      | "proxy";
  }>;

  // ── Event payload (optional, category-specific data) ─────────────
  event_payload?: {
    amount?: number;
    currency?: string;
    reference_id?: string;
    geo_location?: { lat: number; lng: number };
    [key: string]: any;
  };

  // ── Partner intelligence blocks (optional, audit trail only) ─────
  risk_intelligence?: {
    risk_score?: number;
    risk_level?: "low" | "medium" | "high" | "critical";
    risk_factors?: string[];
    model_version?: string;
  };
  compliance_context?: {
    jurisdiction?: string;
    regulatory_flags?: string[];
    screening_results?: Record<string, any>[];
  };
  decision_context?: {
    final_decision?: "approve" | "deny" | "review" | "block" | "escalate";
    decision_timestamp?: string;
    decision_rationale?: string;
    policy_id?: string;
  };

  metadata?: Record<string, any>;
}
```

### استنتاج فئة الحدث تلقائيًا

عند حذف `event_category`، تستنتجها Protegey من `event_type`:

| `event_category` | قيم `event_type` المربوطة تلقائيًا                                                                                                            |
| ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `auth`           | `LOGIN`، `LOGIN_SUCCESS`، `LOGIN_FAILURE`، `SIM_SWAP`، `ACCOUNT_TAKEOVER`، `ACCOUNT_UPDATE`، `MFA_BYPASS`، `PASSWORD_RESET`، `DEVICE_ANOMALY` |
| `transaction`    | `PAYMENT`، `TRANSFER`، `WITHDRAWAL`، `MOBILE_MONEY_SEND`، `CARD_FRAUD`، `PAYMENT_FRAUD`                                                       |
| `kyc`            | `ACCOUNT_CREATE`، `ACCOUNT_OPENING`، `KYC_CHECK`، `DOCUMENT_VERIFY`، `IDENTITY_THEFT`                                                         |
| `fraud_signal`   | كل ما عدا ذلك                                                                                                                                 |

---

## الحقول المطلوبة لكل كاشف (Detector)

تشغِّل Protegey **خمسة كواشف** على كل إشارة. تستلزم بعض الكواشف حقولًا محددة كي تعمل. توفير هذه الحقول يزيد جودة تقييم المخاطر إلى أقصى حد.

| الكاشف                       | الحقل المطلوب                         | مكان تقديمه                                           |
| ---------------------------- | ------------------------------------- | ----------------------------------------------------- |
| `RAPID_TRANSACTION_VELOCITY` | معرف الجهة الفاعلة + الإشارات السابقة | `primary_actor.entity_id`                             |
| `LARGE_OUTLIER_TRANSACTION`  | مبلغ المعاملة                         | `event_payload.amount` + `event_payload.currency`     |
| `SIM_SWAP_PROXIMITY`         | طابع زمني لتغيير SIM                  | `primary_actor.identifiers.sim_changed_at` (ISO 8601) |
| `NEW_DEVICE_ACCESS`          | بصمة الجهاز                           | `primary_actor.device.fingerprint`                    |
| `IMPOSSIBLE_TRAVEL`          | عنوان IP للإرسال                      | `source.ip_address`                                   |

::callout{icon="i-lucide-zap" color="primary"}
**أثرِ كل إشارة**: توفير `source.ip_address` و`primary_actor.device.fingerprint` و`primary_actor.identifiers.sim_changed_at` حيثما ينطبق يزيد دقة الكشف بشكل ملحوظ — لا سيما لأنماط استبدال SIM والاستيلاء على الحسابات الشائعة في احتيال الأموال المحمولة في أفريقيا.
::

---

## معالجة الخصوصية من جانب الخادم

**لا تخزِّن Protegey أي بيانات شخصية (PII) بنص صريح**. أرسل المعرِّفات كنص عادي — يقوم الخادم تلقائيًا بتطبيعها وتجزئتها باستخدام **HMAC-SHA256 مع مفتاح الشبكة المشترك** قبل أي تخزين أو ربط بالرسم البياني.

::callout{icon="i-lucide-shield-alert" color="red"}
**لا تُجزِّئ المعرِّفات مسبقًا.** إرسال قيمة مُجزَّأة مسبقًا يتسبب في تجزئة مزدوجة على الخادم، مما يُعطِّل مطابقة الكيانات بين المؤسسات. أرسل دائمًا معرِّفات خامة وغير معدَّلة.
::

### التطبيع التلقائي

يُطبِّق الخادم تطبيعًا خاصًا بكل نوع قبل التجزئة:

| النوع               | التطبيع المُطبَّق                     | مثال على المدخل        | الشكل القانوني     |
| ------------------- | ------------------------------------- | ---------------------- | ------------------ |
| رقم الهاتف (MSISDN) | E.164 عبر libphonenumber              | `0244 123 456`         | `+233244123456`    |
| البريد الإلكتروني   | حروف صغيرة + إزالة المسافات           | `User@Example.COM `    | `user@example.com` |
| الحساب              | حروف صغيرة + إزالة المسافات           | `ACC-00123456`         | `acc-00123456`     |
| الهوية الوطنية      | أحرف كبيرة + حذف المسافات             | `gh 12 34`             | `GH1234`           |
| BVN                 | أرقام فقط، 11 حرفًا                   | `123-456-789-01`       | `12345678901`      |
| PAN                 | أرقام فقط، 13–19 حرفًا                | `4111 1111 1111 1111`  | `4111111111111111` |
| IMEI                | أرقام فقط، 15 حرفًا                   | `35-387923-456789-0`   | `353879234567890`  |

تُضاف بادئة النوع إلى كل قيمة مطبَّعة قبل التجزئة (مثلاً: `phone:+233244123456`)، مما يضمن أن نوعَين مختلفَين من المعرِّفات اللذَين يُطبَّعان إلى سلسلة نصية واحدة لن ينتجا أبدًا نفس التجزئة.

---

## أمثلة

### إشارة بسيطة (Minimal Signal)

```bash
curl -X POST "https://api.protegey.com/v1/signals" \
  -H "Content-Type: application/json" \
  -H "X-Centry-Key: cen_live_sk_..." \
  -H "Idempotency-Key: $(uuidgen)" \
  -d '{
    "schema_version": "2.0",
    "event_type": "LOGIN",
    "timestamp": "2026-02-25T10:30:00+00:00",
    "source": { "confidence_score": 0.8 },
    "primary_actor": { "entity_id": "user-abc-123" }
  }'
```

### تحويل أموال محمولة كامل (Full Mobile Money Transfer)

```bash
curl -X POST "https://api.protegey.com/v1/signals" \
  -H "Content-Type: application/json" \
  -H "X-Centry-Key: cen_live_sk_..." \
  -H "Idempotency-Key: txn-retry-550e8400" \
  -d '{
    "schema_version": "2.0",
    "event_type": "MOBILE_MONEY_SEND",
    "event_category": "transaction",
    "timestamp": "2026-02-25T10:30:00+00:00",
    "source": {
      "confidence_score": 0.85,
      "integration_channel": "api",
      "ip_address": "41.74.200.50"
    },
    "primary_actor": {
      "entity_id": "user-abc-123",
      "entity_type": "individual",
      "hashed_id": "a3f9b2c1d8e7f6a5b4c3d2e1f0a9b8c7d6e5f4a3b2c1d0e9f8a7b6c5d4e3f2a1",
      "device": {
        "fingerprint": "fp_7d3a9c12_4e88_4b1f_b7d3",
        "user_agent": "Mozilla/5.0 (Linux; Android 13; Pixel 7)"
      },
      "identifiers": {
        "region": "GH"
      },
      "risk_flags": ["high_velocity"]
    },
    "related_entities": [
      {
        "entity_id": "beneficiary-789",
        "entity_type": "individual",
        "relationship_type": "receiver"
      }
    ],
    "event_payload": {
      "amount": 2500.00,
      "currency": "GHS",
      "reference_id": "TXN-20260225-001"
    }
  }'
```

### إشارة كشف استبدال SIM (SIM Swap Detection Signal)

أرسل هذه الإشارة في غضون 24 ساعة من اكتشاف استبدال SIM لتشغيل كاشف `SIM_SWAP_PROXIMITY`:

```bash
curl -X POST "https://api.protegey.com/v1/signals" \
  -H "Content-Type: application/json" \
  -H "X-Centry-Key: cen_live_sk_..." \
  -H "Idempotency-Key: sim-transfer-$(uuidgen)" \
  -d '{
    "schema_version": "2.0",
    "event_type": "TRANSFER",
    "event_category": "transaction",
    "timestamp": "2026-02-25T10:30:00+00:00",
    "source": {
      "confidence_score": 0.9,
      "ip_address": "41.74.200.50"
    },
    "primary_actor": {
      "entity_id": "user-xyz-456",
      "entity_type": "individual",
      "identifiers": {
        "region": "NG",
        "sim_changed_at": "2026-02-25T08:15:00+00:00"
      }
    },
    "event_payload": {
      "amount": 50000.00,
      "currency": "NGN",
      "reference_id": "TXN-20260225-SIM-001"
    }
  }'
```

---

## الاستجابة (Response)

### نجاح (`201 Created`)

استيعاب الإشارة **متزامن** — يعمل تقييم المخاطر الأولي بشكل مباشر ويُعاد فورًا. وهو درجة سريعة تُحتسب عند وقت الاستيعاب بناءً على بيانات الكيان المتاحة. للحصول على تقييم كامل مع مستوى ثقة شامل وتفسيرات، استخدم `POST /risk/assess` مع UUID الكيان.

```json
{
  "success": true,
  "data": {
    "signal_id": "9df4a3b1-c2e3-4f56-a789-b0c1d2e3f456",
    "event_type": "MOBILE_MONEY_SEND",
    "timestamp": "2026-02-25T10:30:00+00:00",
    "risk_score": 0.78,
    "risk_level": "high",
    "detectors_triggered": [
      "RAPID_TRANSACTION_VELOCITY",
      "LARGE_OUTLIER_TRANSACTION"
    ],
    "taxonomy": "mobile_money_fraud"
  },
  "message": "Signal received and assessed"
}
```

### مستويات المخاطر (Risk Levels)

| `risk_level` | نطاق `risk_score` | الإجراء الموصى به               |
| ------------ | ----------------- | ------------------------------- |
| `critical`   | 0.81 – 1.00       | الحجب الفوري وفتح تحقيق         |
| `high`       | 0.51 – 0.80       | طلب تحقق إضافي أو تعليق العملية |
| `medium`     | 0.21 – 0.50       | مصادقة متصاعدة أو مراجعة يدوية  |
| `low`        | 0.00 – 0.20       | السماح — إشارة احتيال منخفضة    |

### الكواشف (Detectors)

| معرف الكاشف                  | ما يكشفه                                                     |
| ---------------------------- | ------------------------------------------------------------ |
| `RAPID_TRANSACTION_VELOCITY` | تكرار معاملات مرتفع بشكل غير معتاد في نافذة زمنية قصيرة      |
| `LARGE_OUTLIER_TRANSACTION`  | مبلغ معاملة يتجاوز بكثير الخط الأساسي التاريخي للجهة الفاعلة |
| `SIM_SWAP_PROXIMITY`         | معاملة عالية القيمة خلال 24 ساعة من تغيير SIM                |
| `NEW_DEVICE_ACCESS`          | نشاط من بصمة جهاز لم يُرَ مسبقًا لهذه الجهة الفاعلة          |
| `IMPOSSIBLE_TRAVEL`          | استحالة جغرافية بين أحداث متتالية                            |

### خطأ في التحقق (`422 Unprocessable Content`)

```json
{
  "message": "The primary_actor.entity_id field is required.",
  "errors": {
    "primary_actor.entity_id": [
      "The primary_actor.entity_id field is required."
    ]
  }
}
```

### أخطاء التحقق الشائعة

| الخطأ                                                                         | السبب                                                   |
| ----------------------------------------------------------------------------- | ------------------------------------------------------- |
| `primary_actor.entity_id is required`                                         | حمولة UFES v2 تفتقر إلى `primary_actor.entity_id`       |
| `event_category must be one of: auth, transaction, kyc, fraud_signal, system` | قيمة فئة غير صالحة — أو احذفها كليًا للاستنتاج التلقائي |
| `flow_type must be one of: kyc, payment, auth`                                | `flow_type` الصريح خارج التعداد المسموح به              |
| `primary_actor.device.fingerprint may not be greater than 255 characters`     | اقطع بصمة الجهاز إلى 255 حرفًا                          |
| `related_entities.0.relationship_type is invalid`                             | نوع علاقة غير معروف                                     |
| `source.integration_channel is invalid`                                       | القناة غير موجودة في `api, batch, stream`               |

---

## الأمانة الإدارية (Idempotency)

استخدم رأس `Idempotency-Key` لإعادة محاولة الطلبات الفاشلة بأمان دون إنشاء إشارات مكررة.

**القواعد:**

- يجب أن يكون المفتاح فريدًا لكل إشارة منطقية (يُوصى باستخدام UUID)
- الحد الأقصى 64 حرفًا
- المفاتيح ذات نطاق الشريك وتنتهي صلاحيتها بعد 24 ساعة
- استجابات `4xx` لا تُخزَّن مؤقتًا — إعادة محاولة حمولة خاطئة لا تعيد تشغيل خطأ مخزن
- عند إعادة التشغيل، تتضمن الاستجابة `X-Idempotency-Replayed: true`

**مثال — إعادة المحاولة مع التراجع التدريجي:**

```javascript
async function submitSignal(payload, idempotencyKey, retries = 3) {
  for (let attempt = 0; attempt < retries; attempt++) {
    try {
      const response = await fetch("https://api.protegey.com/v1/signals", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "X-Centry-Key": process.env.PROTEGEY_SECRET_KEY,
          "Idempotency-Key": idempotencyKey, // same key on every retry
        },
        body: JSON.stringify(payload),
      });

      if (response.ok) return await response.json();
      if (response.status < 500)
        throw new Error(`Client error: ${response.status}`);

      // 5xx — wait and retry
      await new Promise((r) => setTimeout(r, Math.pow(2, attempt) * 1000));
    } catch (err) {
      if (attempt === retries - 1) throw err;
    }
  }
}

// Generate key once, before the first attempt
const signalKey = crypto.randomUUID();
await submitSignal(payload, signalKey);
```

::callout{icon="i-lucide-info" color="blue"}
**أفضل ممارسة**: أنشئ مفتاح الأمانة الإدارية **قبل** إرسال الطلب الأول. خزِّنه إلى جانب الإشارة في نظامك حتى تتمكن من إعادة استخدام نفس المفتاح في أي إعادة محاولة، بغض النظر عن مقدار الوقت المنقضي.
::

---

## توحيد أرقام الهاتف (Phone Normalization)

عند إرسال إشارة تحتوي على رقم هاتف عبر `primary_actor.identifiers.msisdn`، يقوم النظام تلقائيًا بتحويله إلى تنسيق E.164 القياسي قبل التجزئة وربط الرسم البياني.

### الأسواق المدعومة

| الدولة           | كود ISO | أمثلة للتنسيق المحلي          | النتيجة (E.164)  |
| ---------------- | ------- | ----------------------------- | ---------------- |
| **غانا**         | `GH`    | `024 123 4567`، `241234567`   | `+233241234567`  |
| **نيجيريا**      | `NG`    | `0803 123 4567`، `7012345678` | `+2348031234567` |
| **السنغال**      | `SN`    | `077 123 45 67`، `771234567`  | `+221771234567`  |
| **كوت ديفوار**   | `CI`    | `07 01 23 45 67`              | `+2250701234567` |
| **كينيا**        | `KE`    | `0712 345678`                 | `+254712345678`  |
| **جنوب أفريقيا** | `ZA`    | `082 123 4567`                | `+27821234567`   |
| **المغرب**       | `MA`    | `06 12 34 56 78`              | `+212612345678`  |
| **مصر**          | `EG`    | `010 1234 5678`               | `+201012345678`  |

::callout{icon="i-lucide-lightbulb" color="primary"}
**أفضل ممارسة**: قدِّم دائمًا `identifiers.region` للجهة الفاعلة (رمز ISO للدولة) عند إرسال أرقام الهاتف. هذا يضمن دقة توحيد بنسبة 100% للتنسيقات المحلية المتداخلة.
::

### دمج المخاطر المرجَّح في الاستجابة

تتضمن استجابة استيعاب الإشارة كائن `fusion` يوضح كيف احتُسبت درجة المخاطر النهائية عبر الأبعاد الأربعة المرجَّحة:

```json
{
  "risk_score": 78,
  "risk_level": "high",
  "detectors_triggered": ["RAPID_TRANSACTION_VELOCITY"],
  "taxonomy": "payment.structuring",
  "fusion": {
    "behavioral_score": 0.82,
    "network_score": 0.71,
    "contextual_score": 0.55,
    "historical_score": 0.40,
    "fused_score": 0.72
  }
}
```

| البُعد | الوزن | المصدر |
|-----------|--------|--------|
| `behavioral_score` | 35% | تجميع كواشف NUFSS |
| `network_score` | 40% | مخاطر الرسم البياني الساخن المنتشرة (قفزة 1–2) |
| `contextual_score` | 15% | ميزات إثراء المحلل |
| `historical_score` | 10% | متوسط الكيان المتداول خلال 90 يومًا |

تُضرب `fused_score` (0.0–1.0) في 100 لإنتاج `risk_score`.

---

### مرجع الكواشف

تعمل خمسة كواشف على كل إشارة. لكل منها حقول مطلوبة — غياب الحقول يؤدي إلى تخطي الكاشف (لا إلى فشله).

| معرف الكاشف | شرط التشغيل | الحقول المطلوبة |
|-------------|-------------------|-------------------|
| `RAPID_TRANSACTION_VELOCITY` | ≥ 10 معاملات/ساعة لنفس الكيان | `primary_actor.entity_id` |
| `LARGE_OUTLIER_TRANSACTION` | Z-score ≥ 3.0 مقارنةً بخط الأساس التاريخي للكيان (90 يومًا) | `event_payload.amount` + `event_payload.currency` |
| `SIM_SWAP_PROXIMITY` | تغيير جهاز أو موقع خلال ساعتين من حدث استبدال SIM | `primary_actor.identifiers.sim_changed_at` |
| `NEW_DEVICE_ACCESS` | بصمة جهاز تُرى لأول مرة مقترنةً بجغرافيا عالية المخاطر | `primary_actor.device.fingerprint` |
| `IMPOSSIBLE_TRAVEL` | نفس الكيان في دولتين خلال ساعتين | `source.ip_address` (يُثرى بالموقع الجغرافي عبر المحلل) |

يسرد `detectors_triggered` في الاستجابة فقط الكواشف التي **انطلقت** (استوفت حدها). يُقيَّم الكواشف الخمسة دائمًا بصرف النظر عن ذلك.

---
title: دليل استخبارات الكيانات
description: فهم ملفات الكيانات وتقييم المخاطر وتحليل الشبكة
navigation:
  title: استخبارات الكيانات
---

توفر استخبارات الكيانات في Protegey رؤية عميقة في جهات فاعلة الاحتيال وأنماط سلوكها وعلاقاتها الشبكية وتطور مخاطرها عبر الزمن. ابنِ معرفة مؤسسية عن جهات فاعلة الاحتيال واتخذ قرارات مستنيرة بناءً على ملفات كيانات شاملة.

::callout{icon="i-lucide-users" color="purple"}
**اعرف جهاتك الفاعلة**: تتبع جهات فاعلة الاحتيال عبر المعاملات، اكتشف الاتصالات الشبكية، واستفِد من الاستخبارات الجماعية للوقاية من الاحتيال المتكرر.
::

---

## نظرة عامة

### ما هو الكيان؟

**الكيان** يمثل جهة فاعلة فريدة في مجال الاحتيال مُحدَّدة بمعرِّفات مُجزَّأة:

- تجزئة رقم الهاتف
- تجزئة البريد الإلكتروني
- بصمة الجهاز
- عنوان IP
- عنوان محفظة العملة الرقمية

### الكيان مقابل الجهة الفاعلة مقابل الإشارة

| المفهوم       | الوصف             | العدد                    |
| ------------- | ----------------- | ------------------------ |
| **إشارة**     | حدث احتيال منفرد  | معاملة واحدة             |
| **جهة فاعلة** | مشارك في المعاملة | واحدة لكل إشارة          |
| **كيان**      | هوية احتيال فريدة | إشارات متعددة، كيان واحد |

**العلاقة**:

```
Entity (hash_0244567890)
  ├── Signal 1 (TXN-001, Risk: 87)
  ├── Signal 2 (TXN-002, Risk: 92)
  └── Signal 3 (TXN-003, Risk: 45)
```

---

## ملفات الكيانات

### مكونات الملف

**معلومات الهوية**:

- معرف UUID الفريد للكيان
- نوع الكيان (phone، email، device، IP، crypto_address)
- المعرِّف المُجزَّأ (للحفاظ على الخصوصية)
- الاسم المعروض (مُخفَّى لحماية الخصوصية: `0244******`)

**تقييم المخاطر**:

- درجة المخاطر الحالية (0-100)
- مستوى المخاطر (low، medium، high، critical)
- تاريخ المخاطر (تطور الدرجة عبر الزمن)
- درجة الثقة (دقة التنبؤ)

**البيانات السلوكية**:

- إجمالي المعاملات
- تاريخ الاكتشاف الأول
- تاريخ آخر رصد
- أنماط المعاملات
- مقاييس السرعة

**تتبع الحالة**:

- الحالة الحالية (new، monitoring، flagged، confirmed، cleared)
- تاريخ الحالة
- أسباب تغيير الحالة

**استخبارات الشبكة**:

- الكيانات المرتبطة (الجهات الفاعلة المتصلة)
- درجة مخاطر الشبكة
- السمات المشتركة (الجهاز، IP، إلخ)

---

### عرض ملفات الكيانات

**بوابة الشريك**:

1. انتقل إلى استخبارات الكيانات
2. ابحث بالمعرِّف أو UUID
3. اعرض الملف الكامل بالتبويبات:
   - نظرة عامة (درجة المخاطر، الحالة، الملخص)
   - تاريخ المعاملات (جميع الإشارات)
   - الجدول الزمني للمخاطر (تطور الدرجة)
   - رسم الشبكة (الاتصالات)
   - تاريخ الحالة (جميع التغييرات)

**API**:

```typescript
const entity = await client.entities.get("ent_xyz789");

console.log(`Entity: ${entity.display_name}`);
console.log(`Risk Score: ${entity.risk_score}/100`);
console.log(`Status: ${entity.status}`);
console.log(`Total Transactions: ${entity.total_transactions}`);
console.log(`Flagged Count: ${entity.flagged_count}`);
```

---

## دورة حياة حالة الكيان

### تدفق الحالة

```
new → monitoring → flagged → confirmed
                      ↓           ↓
                false_positive  cleared
```

| الحالة             | الوصف                         | متى تستخدم                              |
| ------------------ | ----------------------------- | --------------------------------------- |
| **new**            | كيان مكتشف حديثًا             | أول معاملة مكتشفة                       |
| **monitoring**     | تحت المراقبة                  | مخاطر منخفضة-متوسطة، لم يُتخذ إجراء بعد |
| **flagged**        | نشاط مشبوه مكتشف              | درجة مخاطر عالية، يحتاج تحقيقًا         |
| **confirmed**      | جهة فاعلة احتيال مُحقَّق منها | أكد التحقيق الاحتيال                    |
| **false_positive** | وُضع عليه علم خطأً            | أثبت التحقيق الشرعية                    |
| **cleared**        | كان موضوع علم، الآن آمن       | الاحتيال محلول، الجهة الفاعلة مُؤهَّلة  |

---

### إدارة الحالة

#### وضع علم على الكيان

وضع علامة على الكيان بوصفه مشبوهًا:

```typescript
await client.entities.updateStatus("ent_xyz789", {
  status: "flagged",
  reason:
    "Multiple high-risk transactions detected. Device fingerprint shared with known fraudster.",
  confidence: "high",
});
```

**متى تضع العلم**:

- درجة المخاطر ≥ 80
- إشارات عالية المخاطر متعددة
- ارتباطات شبكية بمحتالين معروفين
- شذوذات سرعة
- تطابقات قائمة حجب

---

#### تأكيد الاحتيال

التحقق من الكيان بوصفه محتالًا مؤكدًا:

```typescript
await client.entities.updateStatus("ent_xyz789", {
  status: "confirmed",
  reason:
    "Investigation confirmed account takeover. Customer verified unauthorized activity. Evidence: case #CS-2026-001",
  actions_taken: [
    "blocked_globally",
    "reversed_transactions",
    "notified_network_partners",
    "law_enforcement_referral",
  ],
});
```

**متى تؤكد**:

- أثبت التحقيق وجود احتيال
- أكد العميل النشاط غير المصرح به
- تم جمع الأدلة (لقطات شاشة، سجلات)
- استُوفي العتبة القانونية
- تم إثبات النمط عبر إشارات متعددة

---

#### وضع علامة خطأ إيجابي

رفع العلم عن كيان وُضع عليه علم خطأً:

```typescript
await client.entities.updateStatus("ent_xyz789", {
  status: "false_positive",
  reason:
    "Verified with customer - high-value purchase was legitimate. Customer provided proof of funds and explained unusual pattern (property purchase).",
  tuning_notes:
    "Consider whitelisting verified high-net-worth customers for large transactions.",
});
```

**متى تضع علامة خطأ إيجابي**:

- أكد العميل أن المعاملة مشروعة
- لم يجد التحقيق مؤشرات احتيال
- سبَّب خطأ تقني وضع علامة خاطئة
- قاعدة العمل كانت عدوانية بشكل مفرط

---

#### رفع العلم عن الكيان

إعادة تأهيل كيان سبق وضع علم عليه:

```typescript
await client.entities.updateStatus("ent_xyz789", {
  status: "cleared",
  reason:
    "Fraud resolved. Customer account recovered and secured. 6 months of clean transaction history since incident.",
  review_date: "2026-08-01", // Optional: schedule periodic review
});
```

**متى ترفع العلم**:

- حل حادثة الاحتيال
- استُعيد الحساب
- تاريخ نظيف ممتد (6+ أشهر)
- تم التحقق من العميل وتأمينه

---

## تقييم المخاطر

### كيف تُحسَب درجات المخاطر

**عوامل المخاطر الأساسية** (0-100):

- أنماط المعاملات (30%)
- مقاييس السرعة (25%)
- الاتصالات الشبكية (20%)
- السلوك التاريخي (15%)
- إشارات الجهاز/IP (10%)

**تعديلات الدرجة**:

- **مُضاعِف الثقة**: 0.8-1.2 بناءً على جودة البيانات
- **تأثير الشبكة**: +0-20 بناءً على ارتباطات الاحتيال
- **وزن التاريخ**: الإشارات الأحدث تحظى بوزن أعلى
- **تعديل الحجم**: عدد معاملات أكبر = درجة أكثر موثوقية

### نطاقات درجة المخاطر

| الدرجة     | مستوى المخاطر | التوصية بالإجراء                     |
| ---------- | ------------- | ------------------------------------ |
| **0-39**   | منخفض         | مراقبة، السماح بالمعاملات            |
| **40-59**  | متوسط         | تحقق معزز (2FA، تأكيد بريد إلكتروني) |
| **60-79**  | عالٍ          | مراجعة يدوية مطلوبة                  |
| **80-89**  | عالٍ جدًا     | حجب مع خيار المراجعة                 |
| **90-100** | حرج           | حجب فوري، تحقيق                      |

---

### تاريخ المخاطر

تتبع كيفية تطور مخاطر الكيان عبر الزمن:

```typescript
const riskHistory = await client.entities.riskHistory("ent_xyz789", {
  start_date: "2025-12-01",
  end_date: "2026-02-03",
});

// Visualize risk trend
riskHistory.data.forEach((point) => {
  console.log(
    `${point.date}: ${point.risk_score} (${point.signal_count} signals)`,
  );
});
```

**حالات الاستخدام**:

- تحديد أنماط الاحتيال المتصاعدة
- التحقق من صحة قرار تأكيد الاحتيال
- رصد تقدم إعادة التأهيل
- تحليل فعالية التدخلات

---

## تحليل الشبكة

### علاقات الكيانات

تُربط الكيانات بناءً على السمات المشتركة:

**أنواع الروابط**:

- **رابط الجهاز**: نفس بصمة الجهاز
- **رابط IP**: نفس عنوان IP أو الشبكة الفرعية
- **رابط سلوكي**: أنماط معاملات مشابهة
- **رابط زمني**: معاملات في أوقات متشابهة
- **رابط جغرافي**: أنماط موقع متشابهة

### اكتشاف الاتصالات الشبكية

**API**:

```typescript
const entity = await client.entities.get("ent_xyz789");

console.log(`Network Risk Score: ${entity.network_risk_score}`);
console.log(`Linked Entities: ${entity.linked_entities_count}`);

// Get linked entities
entity.network_links.forEach((link) => {
  console.log(
    `  - ${link.entity_uuid}: ${link.link_type} (strength: ${link.strength})`,
  );
});
```

**قوة الرابط**:

- **0.9-1.0**: قوي جدًا (سمات مشتركة متعددة)
- **0.7-0.89**: قوي (2-3 سمات مشتركة)
- **0.5-0.69**: متوسط (1-2 سمات مشتركة)
- **< 0.5**: ضعيف (سمة مشتركة واحدة)

---

### اكتشاف حلقات الاحتيال

تحديد شبكات الاحتيال المنسق:

```typescript
async function detectFraudRing(entityUuid) {
  const entity = await client.entities.get(entityUuid);

  // High network risk suggests fraud ring
  if (entity.network_risk_score > 80) {
    console.log("Potential fraud ring detected!");

    // Get all linked entities
    const linkedEntities = await Promise.all(
      entity.network_links.map((link) => client.entities.get(link.entity_uuid)),
    );

    // Count confirmed fraudsters in network
    const confirmedFraudsters = linkedEntities.filter(
      (e) => e.status === "confirmed",
    ).length;

    console.log(`Network size: ${linkedEntities.length}`);
    console.log(`Confirmed fraudsters: ${confirmedFraudsters}`);

    // If multiple confirmed fraudsters, flag entire network
    if (confirmedFraudsters >= 3) {
      for (const linked of linkedEntities) {
        if (linked.status === "new" || linked.status === "monitoring") {
          await client.entities.updateStatus(linked.uuid, {
            status: "flagged",
            reason: `Part of fraud ring with ${confirmedFraudsters} confirmed fraudsters`,
          });
        }
      }
    }
  }
}
```

---

## البحث والتصفية في الكيانات

### البحث عن الكيانات

**بالمعرِّف**:

```typescript
// Search by partial identifier (if unmasked)
const entities = await client.entities.list({
  search: "0244567890",
});
```

**بالحالة**:

```typescript
const flaggedEntities = await client.entities.list({
  status: "flagged",
  sort_by: "risk_score",
  sort_order: "desc",
});
```

**بمستوى المخاطر**:

```typescript
const highRiskEntities = await client.entities.list({
  risk_level: "high",
  min_risk_score: 75,
});
```

**بالنطاق الزمني**:

```typescript
const recentEntities = await client.entities.list({
  first_seen_after: "2026-02-01",
  first_seen_before: "2026-02-03",
});
```

**بحجم المعاملات**:

```typescript
const highVolumeEntities = await client.entities.list({
  min_transactions: 10,
  sort_by: "total_transactions",
  sort_order: "desc",
});
```

---

## إحصاءات الكيانات

### الإحصاءات الإجمالية

```typescript
const stats = await client.entities.statistics({
  start_date: "2026-02-01",
  end_date: "2026-02-03",
});

console.log(`Total Entities: ${stats.total}`);
console.log(`New Entities: ${stats.by_status.new}`);
console.log(`Flagged: ${stats.by_status.flagged}`);
console.log(`Confirmed Fraudsters: ${stats.by_status.confirmed}`);
console.log(`Avg Risk Score: ${stats.avg_risk_score}`);
```

**المقاييس الرئيسية**:

- إجمالي الكيانات المتتبَّعة
- معدل نمو الكيانات
- توزيع الحالات
- متوسط درجة المخاطر
- عدد الكيانات عالية المخاطر
- كثافة الشبكة (الاتصالات لكل كيان)

---

## أفضل الممارسات

### 1. مراجعات الحالة الدورية

**مراجعة الكيانات الموضوع عليها علم شهريًا**:

```typescript
async function reviewFlaggedEntities() {
  const flagged = await client.entities.list({
    status: "flagged",
    flagged_before: thirtyDaysAgo, // Flagged >30 days ago
  });

  for (const entity of flagged.data) {
    // Check recent activity
    const recentSignals = await client.signals.list({
      actor_hash: entity.identifier,
      created_after: thirtyDaysAgo,
    });

    // No recent fraud? Consider clearing
    if (recentSignals.data.every((s) => s.risk_score < 60)) {
      await client.entities.updateStatus(entity.uuid, {
        status: "false_positive",
        reason: "30 days of clean transactions - likely false positive",
      });
    }
  }
}
```

---

### 2. توثيق تغييرات الحالة

**تضمين المبرر دائمًا**:

```typescript
// ❌ BAD: No explanation
await client.entities.updateStatus(entityUuid, {
  status: "confirmed",
});

// ✅ GOOD: Clear documentation
await client.entities.updateStatus(entityUuid, {
  status: "confirmed",
  reason: `Confirmed fraud based on:
- Investigation case #CS-2026-001 completed
- Customer confirmed unauthorized activity
- 3 high-value fraudulent transactions (GHS 45,000 total)
- Device fingerprint matches 2 other known fraudsters
- Evidence: transaction logs, customer communication, law enforcement report`,
  evidence_case_id: "CS-2026-001",
  actions_taken: [
    "blocked_globally",
    "reversed_transactions",
    "notified_partners",
    "law_enforcement_case_filed",
  ],
});
```

---

### 3. الاستفادة من استخبارات الشبكة

**استخدام الاتصالات الشبكية للكشف المبكر**:

```typescript
async function monitorNetworkRisk(entityUuid) {
  const entity = await client.entities.get(entityUuid);

  // Check if connected to known fraudsters
  for (const link of entity.network_links) {
    const linkedEntity = await client.entities.get(link.entity_uuid);

    if (linkedEntity.status === "confirmed" && link.strength > 0.7) {
      console.warn(
        `Entity linked to confirmed fraudster (strength: ${link.strength})`,
      );

      // Auto-flag if strong connection
      if (entity.status === "new") {
        await client.entities.updateStatus(entityUuid, {
          status: "flagged",
          reason: `Strong network link (${link.strength}) to confirmed fraudster ${linkedEntity.uuid}`,
        });
      }
    }
  }
}
```

---

### 4. تتبع تطور المخاطر

**مراقبة الكيانات ذات المخاطر المتصاعدة**:

```typescript
async function detectEscalatingRisk() {
  const entities = await client.entities.list({
    status: "monitoring",
    min_risk_score: 50,
  });

  for (const entity of entities.data) {
    const history = await client.entities.riskHistory(entity.uuid, {
      start_date: thirtyDaysAgo,
      end_date: today,
    });

    // Calculate risk trend
    const firstScore = history.data[0].risk_score;
    const lastScore = history.data[history.data.length - 1].risk_score;
    const increase = lastScore - firstScore;

    // Flag if risk increased significantly
    if (increase > 20) {
      console.warn(
        `Entity ${entity.uuid} risk increased by ${increase} points`,
      );

      await client.entities.updateStatus(entity.uuid, {
        status: "flagged",
        reason: `Risk score increased from ${firstScore} to ${lastScore} over 30 days`,
      });
    }
  }
}
```

---

### 5. الحفاظ على نظافة البيانات

**تنظيف دوري**:

```typescript
async function cleanupLowRiskEntities() {
  // Clear old low-risk entities that haven't transacted recently
  const staleEntities = await client.entities.list({
    status: "monitoring",
    max_risk_score: 30,
    last_seen_before: sixMonthsAgo,
  });

  for (const entity of staleEntities.data) {
    // Archive or clear status
    await client.entities.updateStatus(entity.uuid, {
      status: "cleared",
      reason: "Cleanup: Low risk, no activity for 6 months",
    });
  }
}
```

---

### 6. التقاطع مع الحالات

**ربط الكيانات بحالات التحقيق**:

```typescript
async function investigateEntity(entityUuid) {
  const entity = await client.entities.get(entityUuid);

  // Check if entity is involved in existing cases
  const relatedCases = await client.cases.list({
    search: entity.identifier,
  });

  if (relatedCases.data.length > 0) {
    console.log(`Entity involved in ${relatedCases.data.length} cases`);

    // Review case outcomes for entity status
    const resolvedCases = relatedCases.data.filter(
      (c) => c.status === "resolved",
    );

    if (resolvedCases.some((c) => c.resolution.includes("Confirmed fraud"))) {
      // Update entity status based on case findings
      await client.entities.updateStatus(entityUuid, {
        status: "confirmed",
        reason: `Confirmed fraud in case ${resolvedCases[0].uuid}`,
        evidence_case_id: resolvedCases[0].uuid,
      });
    }
  }
}
```

---

### 7. استخدام الحالة لاتخاذ القرارات

**قواعد القرار الآلي بناءً على حالة الكيان**:

```typescript
async function makeTransactionDecision(signal) {
  // Check entity status
  const entities = await client.entities.list({
    search: signal.actor.hashed_id,
  });

  if (entities.data.length > 0) {
    const entity = entities.data[0];

    switch (entity.status) {
      case "confirmed":
        // Auto-block confirmed fraudsters
        return {
          decision: "declined",
          reason: "Known fraudster - Entity status: confirmed",
        };

      case "flagged":
        // Require additional verification
        return {
          decision: "review",
          reason: "Flagged entity - Manual review required",
        };

      case "false_positive":
        // Lower threshold for previously cleared entities
        if (signal.risk_score < 70) {
          return {
            decision: "approved",
            reason: "Previously false positive, acceptable risk",
          };
        }
        break;

      case "cleared":
        // Trust rehabilitated entities slightly more
        if (signal.risk_score < 65) {
          return {
            decision: "approved",
            reason: "Cleared entity with acceptable risk",
          };
        }
        break;
    }
  }

  // Default: Use risk score
  return signal.risk_score >= 80 ? "declined" : "approved";
}
```

---

## السيناريوهات الشائعة

### السيناريو الأول: كيان جديد عالي المخاطر

```typescript
async function handleNewHighRiskEntity(signal) {
  // 1. Check if entity exists
  let entity = await client.entities.list({
    search: signal.actor.hashed_id,
  });

  if (entity.data.length === 0) {
    console.log("New entity detected with high risk");

    // Wait for entity to be created by system
    await sleep(2000);

    entity = await client.entities.list({
      search: signal.actor.hashed_id,
    });
  }

  const entityData = entity.data[0];

  // 2. Flag if high risk
  if (signal.risk_score >= 80) {
    await client.entities.updateStatus(entityData.uuid, {
      status: "flagged",
      reason: `First transaction high risk (score: ${signal.risk_score}). Risk factors: ${signal.risk_factors.map((f) => f.factor).join(", ")}`,
    });

    // 3. Create case for investigation
    await client.cases.createFromSignal(signal.id, {
      priority: "high",
      category: signal.event_type,
      description: `New entity flagged with high-risk first transaction`,
    });
  }
}
```

---

### السيناريو الثاني: اكتشاف حلقة احتيال

```typescript
async function investigateFraudRing(suspectedEntityUuid) {
  // 1. Get suspected entity
  const entity = await client.entities.get(suspectedEntityUuid);

  // 2. Get network connections
  const networkEntities = await Promise.all(
    entity.network_links
      .filter((link) => link.strength > 0.7) // Strong links only
      .map((link) => client.entities.get(link.entity_uuid)),
  );

  // 3. Analyze network
  const confirmedFraudsters = networkEntities.filter(
    (e) => e.status === "confirmed",
  );
  const flaggedEntities = networkEntities.filter((e) => e.status === "flagged");

  console.log(`Network Analysis:
Total Entities: ${networkEntities.length}
Confirmed Fraudsters: ${confirmedFraudsters.length}
Flagged: ${flaggedEntities.length}
Avg Risk Score: ${networkEntities.reduce((sum, e) => sum + e.risk_score, 0) / networkEntities.length}`);

  // 4. If fraud ring pattern detected
  if (confirmedFraudsters.length >= 2) {
    console.log("Fraud ring detected - flagging entire network");

    // Create master case
    const masterCase = await client.cases.create({
      title: `Fraud Ring Investigation - ${networkEntities.length} Connected Entities`,
      priority: "critical",
      category: "FRAUD_RING",
      description: `Network of ${networkEntities.length} entities with ${confirmedFraudsters.length} confirmed fraudsters`,
    });

    // Flag all connected entities
    for (const connectedEntity of networkEntities) {
      if (
        connectedEntity.status === "new" ||
        connectedEntity.status === "monitoring"
      ) {
        await client.entities.updateStatus(connectedEntity.uuid, {
          status: "flagged",
          reason: `Part of fraud ring with ${confirmedFraudsters.length} confirmed fraudsters. Investigation case: ${masterCase.uuid}`,
        });
      }
    }
  }
}
```

---

### السيناريو الثالث: إعادة تأهيل الكيان

```typescript
async function rehabilitateEntity(entityUuid) {
  const entity = await client.entities.get(entityUuid);

  // Check if entity has been clean for 6 months
  const recentSignals = await client.signals.list({
    actor_hash: entity.identifier,
    created_after: sixMonthsAgo,
  });

  const allClean = recentSignals.data.every((s) => s.risk_score < 50);
  const avgRiskScore =
    recentSignals.data.reduce((sum, s) => sum + s.risk_score, 0) /
    recentSignals.data.length;

  if (allClean && entity.status === "flagged") {
    console.log(`Entity ${entityUuid} has 6 months clean history`);

    await client.entities.updateStatus(entityUuid, {
      status: "cleared",
      reason: `Entity rehabilitation - 6 months clean transaction history. Avg risk score: ${avgRiskScore.toFixed(1)}/100. Total clean transactions: ${recentSignals.data.length}`,
      review_date: moment().add(3, "months").format("YYYY-MM-DD"), // Schedule follow-up review
    });
  }
}
```

---

## المقاييس والتقارير

### لوحة تحكم استخبارات الكيانات

**المقاييس الرئيسية**:

- إجمالي الكيانات المتتبَّعة
- توزيع الحالات (new، monitoring، flagged، confirmed)
- متوسط درجة المخاطر
- عدد الكيانات عالية المخاطر
- كثافة الشبكة
- معدل تأكيد الاحتيال
- معدل الخطأ الإيجابي
- معدل نمو الكيانات

**مثال على لوحة التحكم**:

```typescript
async function buildEntityDashboard() {
  const stats = await client.entities.statistics({
    start_date: thirtyDaysAgo,
    end_date: today,
  });

  return {
    overview: {
      total: stats.total,
      new_this_month: stats.new_count,
      high_risk: stats.by_risk_level.high + stats.by_risk_level.critical,
    },
    by_status: stats.by_status,
    by_risk_level: stats.by_risk_level,
    network_stats: {
      avg_connections: stats.avg_network_connections,
      fraud_rings: stats.fraud_ring_count,
    },
  };
}
```

---

## وثائق ذات صلة

- [واجهة API لاستخبارات الكيانات](../api-reference/entities) - مرجع API الكامل
- [مراقبة المعاملات](./transaction-monitoring) - ربط الإشارة بالكيان
- [إدارة الحالات](./case-management) - سير عمل تحقيق الكيانات
- [إدارة التنبيهات](./alert-management) - تهيئة التنبيهات القائمة على الكيانات

::callout{icon="i-lucide-network" color="purple"}
**تأثير الشبكة**: كلما زاد عدد الشركاء المُسهمين في Protegey، ازدادت قوة استخبارات الكيانات. الأنماط المشتركة عبر المؤسسات تُعزِّز الكشف عن الاحتيال للجميع.
::
